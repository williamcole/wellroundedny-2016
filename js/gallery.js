/*******************/
/* GALLERY SCRIPTS */
/*******************/

jQuery(document).ready(function($) {
	
	// NO LONGER NEEDED ???
	
	/*
	
	if( $(window).width() > 800 ) {
    	
    	// gallery text scrollbar
		$('.scroll-pane').jScrollPane({
			verticalDragMinHeight: 40,
		    verticalDragMaxHeight: 40,
		    horizontalDragMaxWidth: 0,
		    autoReinitialise: true,
		    autoReinitialiseDelay: 0,
		    animateScroll: true
	    });
    
	    // get heights for calculation
		scrollpane_height = $('.scroll-pane:first').height();
		pager_height = $('.cycle-pager:first').height();
		image_area_height = scrollpane_height - pager_height;
		
		// vertically center small images
		$('.cycle-image img').each(function() {
			
			image_height = $(this).attr('img-height');
			
			if( image_height < image_area_height ) {
				diff = image_area_height - image_height;
				if( diff > 50 ) {
					$(this).css( 'top', diff/2 );
				}
			}
			
		});
	
	} else {
	
		// gallery text scrollbar
		$('.scroll-pane').jScrollPane({
			verticalDragMinHeight: 40,
		    verticalDragMaxHeight: 40,
		    horizontalDragMaxWidth: 0,
		    autoReinitialise: true,
		    autoReinitialiseDelay: 0,
		    animateScroll: true
	    });
		
	}
	
	*/
	
})